import {Button, Input, InputGroup, InputGroupAddon, InputGroupText} from "reactstrap";
import React, {PureComponent} from "react";

class CoordinateList extends PureComponent {

    render() {
        return (
            
        )
    }
}

export default CoordinateList;
